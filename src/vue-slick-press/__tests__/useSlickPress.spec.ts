import { describe, it, expect } from 'vitest';
import { useSlickPress } from '../index';

describe('useSlickPress', () => {
  it('dummy', () => {
    expect('needs testing').toBe('needs testing');
  });
});
